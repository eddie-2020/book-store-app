import Header from '../components/Header';

const Categories = () => (
  <div>
    <Header />
    <div style={{ height: 2, backgroundColor: '#efefef', marginBottom: 30 }} />
    <h2 style={{ textAlign: 'center', fontFamily: 'cursive' }}>This Page Is Under Contruction</h2>
  </div>
);

export default Categories;
